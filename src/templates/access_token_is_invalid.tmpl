{{ template "generic_page" . -}}

{{ define "title" }}Unauthorized{{ end -}}

{{ define "header" }}Provided Credentials Are Invalid{{ end -}}

{{ define "contents" }}<p>Your <code>access_token</code> wasn't found in the database - it must have expired. To get a new token, sign in on the <a href="/">index page</a>.</p>{{ end -}}
